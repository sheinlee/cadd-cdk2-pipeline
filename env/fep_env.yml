name: fep
channels:
  - conda-forge
dependencies:
  # OpenFE pulls OpenMM, openmmtools, openff, gufe, lomap/kartograf, pymbar, etc.
  - openfe
  # pin the CUDA stack to the GPU driver (RTX 4090, driver CUDA 12.6)
  - cuda-version=12.6
